# Private pilot notice

This repository is prepared for the University Press pilot and named collaborators. No public software license is granted by this repository. The two packages under `vendor/` retain their upstream ownership and are pinned to repositories owned by `tuckerebc-max`.

Do not commit manuscripts, author packets, API keys, the `data/` directory, or run outputs. Review licensing and press policy before making the repository public.
